# Session C execution handoff

The user explicitly asked to start implementing Session C and run it in tmux. You are the sole implementation owner of this session. Work in /Users/victor/Projects/hackathons/cairn.

Read sessions/C.md, CONTRACTS.md, CLAUDE.md, INTEGRATION.md and .omx/plans/session-c-independent-plan.md. Execute the C-first plan using the scaffold already in this repository. A and B have not started. Begin with the independent evaluation corpus, C0 cost instrumentation and spending guard, then C1 and the remaining C phases. Do not wait for A or B for work that can be tested independently. Do not claim integrated acceptance before real data/integration exists.

Critical user instructions:
- DO NOT ACCESS .env AT ALL WHATSOEVER. Do not read, write, source, inspect or search it, and do not run commands that implicitly load it. Avoid broad file searches without excluding all .env variants. In particular Next dev/build and Docker Compose may auto-load environment files; do not invoke their defaults. Do not dump process environment or credentials. Do not modify git hooks or shell startup files.
- Authorized maximum API spending is $500 total for this work. This is NOT a monthly renewal or a target to spend. Account-side enforcement has NOT been configured. Make NO paid project API calls until there is verified spending protection and a permitted way to receive credentials without reading .env. Build and test with mocks in the meantime. Do not invent prices or assume the scaffold pricing is current. Never use unpriced models with a zero-cost default. Budget design must account for concurrent requests, in-flight reservations and ambiguous failures; coordinate shared DB changes with A rather than silently modifying A-owned schema.
- Tell the user BEFORE any particularly important or large decision. The user has NOT said they are leaving. Pause for input for material architecture changes, shared-contract/schema changes, new dependencies, destructive actions, or expanded scope. Continue independent work while awaiting input. Routine reversible implementation choices within the plan are authorized.

Ownership: packages/intel and apps/web/app/api/intel handlers; /api/search is explicitly C-owned in CONTRACTS. Do not edit A's worker/contracts/schema or B's reader. Record cross-owner needs in INTEGRATION.md. No new dependencies without explicit user request. Preserve fixture mode and frozen types. You are not alone in the repository; preserve others' changes. Current untracked package-lock.json is pre-existing user work, leave it alone. .omx includes planning artifacts from the parent session.

Use direct execution, not OMX team mode. If useful, native subagents may handle bounded independent tasks with explicit ownership; do not recursively spawn tmux teams. Create sess/c-intel if still on main and safe to do so. No commits or pushes needed. If you do make an explicitly requested commit later, follow Lore intent-first messages and git trailers.

Local prerequisites checked: Node v25.9.0, pnpm 10.15.1, Docker CLI 29.5.2. Docker daemon socket was missing on last check; do not assume it is now running. Dependencies were not yet fully installed (root node_modules had only TypeScript binaries). No code implemented yet. Root codex CLI was broken; this tmux session uses isolated Codex 0.120.0 in /private/tmp/cairn-c-codex.

Work with frequent concise updates. Run focused tests and typecheck; do not claim unavailable checks passed. Add regression tests where behavior is consequential (budget, accounting, grounding, merges). First useful milestone: independent fixtures + 50-call synthetic ledger test + one validated card persisted through bake with safe fallback. Keep .omx/plans/session-c-progress.md current with completed tasks, evidence, pending decisions and blockers so the parent chat can inspect it. Do not write secrets there. On completion report changed files, verification and remaining risks.
